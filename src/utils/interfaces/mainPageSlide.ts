export interface MainPageSlide {
  // id: number;
  btnBackgroundColor: string;
  btnColor: string;
  background: string;
  image: string;
  text: string;
  btnText: string;
  secondaryText: string;
}